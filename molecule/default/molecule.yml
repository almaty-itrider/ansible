---
driver:
  name: podman
platforms:
  - name: rhel8
    image: molecule-rhel8:latest
    dockerfile: Dockerfile
    command: "/usr/sbin/init"
    pre_build_image: false
    capabilities:
      - SYS_ADMIN
    volumes:
      - /sys/fs/cgroup:/sys/fs/cgroup:ro
